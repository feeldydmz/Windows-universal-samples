<UserControl x:Class="Megazone.HyperSubtitleEditor.Presentation.View.SignInView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:attachedProp="clr-namespace:Megazone.HyperSubtitleEditor.Presentation.Infrastructure.AttachedProperty;assembly=Megazone.HyperSubtitleEditor.Presentation.Infrastructure"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             DataContext="{Binding Source={StaticResource SubtitleViewModelLocator}, Path=SignIn}">
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding Path=LoadCommand}" />
        </i:EventTrigger>
        <i:EventTrigger EventName="clo" />
    </i:Interaction.Triggers>
    <Grid>
        <WebBrowser Name="WebBrowser"
                    attachedProp:WebBrowserAttachedProperty.UriSource="{Binding UriSource, Mode=TwoWay}"
                    attachedProp:WebBrowserAttachedProperty.NavigatingCommand="{Binding NavigatingCommand}" />
    </Grid>
</UserControl>